## 1. 專案基礎設定

- [x] 1.1 更新 Cargo.toml，加入所有依賴（ratatui, crossterm, serde, serde_json, rayon, anyhow, windows-sys/nix）
- [x] 1.2 建立模組目錄結構（platform/, process/, memory/, scan/, address/, ui/）與 mod.rs 檔案
- [x] 1.3 定義共用錯誤型別（src/error.rs）

## 2. 平台抽象層

- [x] 2.1 定義 Platform trait（程序列舉、程序附加、記憶體讀寫、記憶體區域列舉）
- [x] 2.2 實作 Windows 平台模組（windows-sys: CreateToolhelp32Snapshot, OpenProcess, ReadProcessMemory, WriteProcessMemory, VirtualQueryEx）
- [x] 2.3 實作 Linux 平台模組（/proc 檔案系統, process_vm_readv, process_vm_writev）
- [x] 2.4 建立平台自動選擇機制（條件編譯 #[cfg]）

## 3. 程序管理

- [x] 3.1 實作程序列舉功能，回傳 ProcessInfo（PID、名稱）列表
- [x] 3.2 實作程序附加功能，取得 ProcessHandle
- [x] 3.3 實作程序名稱篩選（不分大小寫）

## 4. 記憶體讀寫模組

- [x] 4.1 實作記憶體區域列舉，回傳 MemoryRegion（基址、大小、保護屬性）列表
- [x] 4.2 實作記憶體讀取函式（讀取指定位址的原始位元組）
- [x] 4.3 實作記憶體寫入函式（寫入位元組到指定位址）
- [x] 4.4 為讀寫操作加入錯誤處理（無效位址、權限不足）

## 5. 掃描數值型別系統

- [x] 5.1 定義 ScanValue enum（U8, U16, U32, U64, I8, I16, I32, I64, F32, F64, Bytes, String）
- [x] 5.2 實作各型別的序列化/反序列化（從原始位元組轉換）
- [x] 5.3 實作各型別的比較邏輯（等於、大於、小於、不等於）
- [x] 5.4 定義 ScanType enum（ExactValue, UnknownInitial, Increased, Decreased, Changed, Unchanged）

## 6. 記憶體掃描引擎

- [x] 6.1 實作首次精確值掃描（遍歷所有可讀記憶體區域，找出符合值的位址）
- [x] 6.2 實作首次未知初始值掃描（記錄記憶體快照）
- [x] 6.3 實作後續篩選掃描（在已有結果中搜尋）
- [x] 6.4 實作變化比較篩選（增加/減少/不變/已改變）
- [x] 6.5 使用 rayon 實作多執行緒平行掃描
- [x] 6.6 實作掃描進度回報機制（回呼或 channel）
- [x] 6.7 實作掃描重設功能

## 7. 位址表管理

- [x] 7.1 定義 AddressEntry 結構（位址、型別、描述、值、鎖定狀態）
- [x] 7.2 實作位址表 CRUD 操作（新增、刪除、編輯描述）
- [x] 7.3 實作即時數值更新（定期讀取並更新所有項目的值）
- [x] 7.4 實作數值鎖定功能（定期覆寫記憶體值）
- [x] 7.5 實作數值編輯（寫入新值到目標記憶體）
- [x] 7.6 實作 cheat table 儲存（序列化為 JSON）
- [x] 7.7 實作 cheat table 載入（從 JSON 反序列化）

## 8. TUI 應用程式框架

- [x] 8.1 建立應用程式主結構 App（狀態、事件循環）
- [x] 8.2 實作終端初始化與清理（替代螢幕、raw mode）
- [x] 8.3 實作事件處理迴圈（鍵盤事件、定時更新事件）
- [x] 8.4 實作畫面路由（一般模式 vs 輸入模式）
- [x] 8.5 實作狀態列元件（程序資訊、快捷鍵提示、錯誤訊息）

## 9. TUI 程序選擇畫面

- [x] 9.1 實作程序列表顯示元件（PID、名稱，可捲動）
- [x] 9.2 實作篩選欄位與即時篩選
- [x] 9.3 實作重新整理功能
- [x] 9.4 實作選擇並附加程序的操作流程

## 10. TUI 記憶體掃描畫面

- [x] 10.1 實作掃描控制區域（型別選擇下拉、值輸入欄位、掃描/篩選/重設按鈕）
- [x] 10.2 實作掃描結果列表（位址、值，可捲動）
- [x] 10.3 實作掃描進度條顯示
- [x] 10.4 實作從結果新增到位址表的操作

## 11. TUI 位址表畫面

- [x] 11.1 實作位址表列表元件（位址、型別、描述、值、鎖定狀態）
- [x] 11.2 實作鎖定切換操作
- [x] 11.3 實作值編輯彈出輸入
- [x] 11.4 實作儲存/載入 cheat table 的檔案路徑輸入

## 12. TUI Hex Viewer 畫面

- [x] 12.1 實作十六進位顯示元件（位址 | hex | ASCII 格式）
- [x] 12.2 實作上下捲動瀏覽
- [x] 12.3 實作位址跳轉輸入
- [x] 12.4 實作即時更新與變化位元組高亮

## 13. 整合與測試

- [x] 13.1 整合所有模組，確保 main.rs 正確啟動完整應用程式
- [x] 13.2 為掃描引擎撰寫單元測試（各型別掃描、篩選邏輯）
- [x] 13.3 為位址表序列化/反序列化撰寫單元測試
- [x] 13.4 為平台抽象層撰寫整合測試（讀寫自身程序記憶體）
- [x] 13.5 在 Windows 上進行端對端測試
